# Base URL of the Invo API (no trailing slash, no /api/v1 suffix).
# Single-origin deploy: leave empty so the SPA calls the same host that served it.
# Local dev against the Vite dev server: point at the backend port.
VITE_API_URL=http://localhost:9025
